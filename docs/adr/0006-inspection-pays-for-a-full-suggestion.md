# Inspection pays for a full Suggestion

`hunch inspect` prints the Champion Suggestion, the command-ngram guess, the first-byte top 8, and bits per byte of that Suggestion, then appends one record in the state directory. Daily use still stops at 200ms. First-byte-only Inspection was rejected because the record would describe a Suggestion the user never saw. `hunch stats` still stores no command text.
