# First-run is install, setup, then a new prompt

First-run is at most three steps after the user has this repo. `uv tool install .` puts the command on the machine. `hunch setup` trains and writes the hook. The next prompt is the first suggestion. A separate aliases edit was an extra step people skipped. Daily use after that has no further action.
