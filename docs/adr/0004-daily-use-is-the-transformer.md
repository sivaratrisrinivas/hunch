# Daily use is the transformer

The Scoreboard is the transformer's exact-command accuracy. Daily use shows that same model. Command-ngram still trains and still prints its number. It never becomes the Champion.

Serving whichever adapter won exact-command accuracy was rejected. The prompt and the Scoreboard would disagree, and you would watch the table while thinking you were watching the language model.
